## 1. Цели вертикального среза

- ✅ Боевка, квесты, лут, уровень и базовый HUD уже прототипированы (см. чек-лист статусов для боевой, квестовой, лутовой и зональной систем).

- 🟡 Сформировать однотактный игровой цикл “взял квест → выполнил → вернулся” в конкретной зоне: связать стартового NPC с квест-цепочкой и переносом игрока в боевую область через существующие `ChangeScene` триггеры (`src/server/ecs/components/trigger/change-scene.ts`) и сцену `WorldScene` на клиенте (`src/client/core/scene/world-scene.ts`).

## 2. Объём вертикального среза

- ❌ Завести набор мобов (3–5 обычных + мини-босс) с параметрами уровня/поведения: добавить шаблоны в загрузчик сцены (`src/server/core/scene/dynamicly-loadable-scene.ts`) и соответствующие конфиги в базе, используя существующие компоненты поведения/агро.

- 🟡 Описать стартовую зону (~200×200 тайлов) с лагерем, мини-локациями и границами в `shared/maps/mapping.ts` и конфигурации сцены, дополнив текущий набор тестовых карт.

- ❌ Заполнить контентом: 2–4 квеста (ToHave/ToKill/ToVisit) и предметы 1–3 уровней; добавить их записи в Mongo через генераторы (`src/tools/json/quest-json-generator.ts`, `src/tools/json/character-json-generator.ts`).

## 3. Система персонажа

- 🟡 Расширить `main-stats` компонент выносливостью и связать её с расчётом HP/ресурсов (добавить поля и перерасчёт в `src/server/ecs/components/game/stats/main-stats.ts` и `health/resource` компонентах).

- 🟡 Дополнить производные характеристики (скорость атаки, резисты) в `secondary-stats` и `speed`, а также обеспечить обновление при апе уровня, экипировке и способностях через систему статов/экипировки.

- 🟡 Внедрить единую формулу урона/критов/регена, интегрировав её в `DamageSystem` и `HealthSystem` согласно дизайну формул.

## 4. Прогрессия (уровни 1–5)

- ✅ XP-кап и автолевел уже реализованы через `LVL_CAPS` и компонент `Level`.

- 🟡 Добавить источники XP за убийство мобов и события сцены: при смерти врага начислять опыт через `Level.addExp` в системах боя (`damage`/`resurrection`).

- ❌ Реализовать награды за уровень (статы, способности, доступ к мини-боссу) в `LevelSystem`, чтобы при достижении 2–5 уровней автоматически добавлялись статы/умения/триггеры контента.

## 5. Класс «Воин» и способности

- 🟡 Зафиксировать класс игрока через `class` компонент (воин) и описать механику ярости (генерация автоатаками/способностями) в `resource` компоненте и `Spell` базовом классе, чтобы Hit/Heavy/Gap/Bash использовали один ресурсный пул.

- 🟡 Собрать минимальный набор активных/пассивных умений: использовать существующие `Hit`, `Shot`, `Dot` как основу, добавить Heavy Strike, Charge, Block, AoE и 2 пассивки (броня, урон оружием) в `src/server/mechanics/spells/warrior`. Обеспечить кулдауны и стоимость ресурса через уже готовую систему кулдаунов.

- ❌ Прописать связь стартового класса и стартовых способностей/экипировки в `spell-book` и `inventory` генерации персонажа, чтобы новый воин автоматически получал нужный набор.

## 6. Бой и формулы

- ✅ Базовые системы `CastRequest`, `Cast`, `Damage`, DoT/HoT и агро уже обеспечивают прототип боёвки.

- 🟡 Ввести честные расстояния/оружейные зоны: обобщить проверку `Spell.range` в `cast` и добавить ограничения по типу оружия (меле/дистанция).

- ❌ Настроить реген вне/в бою (5% HP) через `HealthSystem` и состояния `Combat` компонента, а также добавить формулу брони/критов из дизайна в урон оружия.

## 7. Мобы и их логика

- 🟡 Имеются агро- и патрульные деревья, но нет типизации мобов. Создать шаблоны Bandit/Beast/Spirit/Swamp/mini-boss с разными наборами поведений и спеллов, используя существующий `AggroTree` и расширяя `Behavior` конфиги.

- ❌ Настроить выпадение лута у мобов: при смерти NPC создавать `Loot` контейнеры по их таблице (расширить `LootSystem` и `rollLoot`).

- ❌ Добавить уровни мобов/мини-босса и масштабирование HP/урона, чтобы игрок ощущал прогрессию к уровню 5.

## 8. Квесты и цепочка VS

- ✅ Принимать/сдавать квесты можно через `QuestSystem`, `QuestBook`, UI `QuestBookUI`/`QuestRequestUI`.

- 🟡 Добавить обработку `ToKill` требований: в `QuestRequirementSystem` подписаться на события смерти NPC, инкрементировать прогресс и триггерить завершение.

- 🟡 Создать конкретную цепочку из 4 квестов (“Пропавший разведчик”, “Опасность в лесу”, “Трофеи”, “Бой с Туманным порождением”) с условиями допуска и наградами (XP, предметы) через генератор `quest-json-generator.ts` и Mongo.

## 9. Предметы, инвентарь и лут

- ✅ Инвентарь/экипировка и окно лута уже присутствуют в UI и компонентах (`InventoryUI`, `equip`).

- ❌ Ввести типы/качество/требования предметов: расширить базовый `Item` и `EquipItem` схемы полями slotType/quality/requiredLevel/class и проверять их в `Equip`.

- 🟡 Настроить уровневые предметные наборы (оружие/броня/аксессуары 1–3 уровня) и добавить их в лут-таблицы мобов/квестов.

- ❌ Реализовать лут-дропы у мобов и гарантированный редкий предмет с мини-босса (расширить `rollLoot` и смерть NPC).

## 10. Зона «Приграничный Лес»

- 🟡 Клиент уже умеет загружать карты, но нужно подготовить конкретный тайлсет 200×200 с лагерем, лесной зоной, локацией визита и ареной мини-босса, подключив его к `WorldScene`.

- ❌ Обозначить безопасные/боевые сектора, расставить спавны мобов, объекты для ToHave квестов и переходы между участками через триггеры.

- ❌ Добавить ночной туман/статику (кусты/камни) в слой карты и синхронизировать их с клиентскими ассетами.

## 11. UI / UX

- ✅ Панель способностей, инвентарь, окно лута, журнал квестов и базовый статус-бар уже есть (`SpellBar`, `InventoryUI`, `QuestBookUI`, `StatusBar`).

- ❌ Создать панель персонажа (уровень, статы, экипировка) с использованием компонентов `QuestBook`, `equip`, `main-stats` на клиенте.

- ❌ Реализовать квестовые диалоги с описанием цепочек (расширить `QuestDialog` компонент).

- 🟡 Полировать HUD (иконки ресурсов, всплывающие подсказки урона) для завершённости VS.

## 12. Сохранение

- ✅ Игроки, инвентарь и позиции сохраняются через Mongo (`writePlayer`, `Inventory.serialize`).

- 🟡 Сохранить прогресс квестов (требования) – расширить `QuestBook.serialize` и запись в БД, чтобы при перезаходе сохранялся прогресс ToHave/ToKill/ToVisit.

- 🟡 Добавить слоты сохранения экипировки (уже сериализуется, но нужно убедиться, что новый лут из VS корректно восстанавливается).

- ❌ Подготовить миграции/скрипты для автоматической инициализации VS-персонажей/квестов.

## 13. Технические требования

- ✅ Сервер использует Colyseus ECS с системами Cast/Cooldown/Movement/Combat/Level, клиент — Phaser HUD/WorldScene, что соответствует требованию платформы.

- 🟡 Обновить `app.config.ts` и сцены так, чтобы VS-зона грузилась по умолчанию и выдерживала 30+ FPS (профилировать WorldScene, оптимизировать количество сущностей).

- ❌ Создать smoke-тесты стабильности: автоматический прогон спавна мобов, квестов и сохранений.

## 14. Критерии готовности VS

- ✅ Большинство пунктов (движение, бой, XP, лут, квесты) уже базово выполняются по текущему прототипу, но нет мини-босса/роста силы через предметы.

- ❌ Проверить end-to-end сценарий: создать тестового игрока, пройти цепочку из 4 квестов, одолеть мини-босса, получить редкий предмет и убедиться, что характеристики заметно выросли (настройка предметов/статов).

- 🟡 Задокументировать и автоматизировать smoke-прохождение (скрипты QA, демо-видео).

## 15. Следующие шаги после VS

- ❌ Подготовить технический долг-лист для “после VS”: второй класс, подземелье, расширение зоны до 10 уровня, профессии и мировые события — описать требования в отдельном roadmap, но начать их только после закрытия пунктов 1–14.

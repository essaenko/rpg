import { Component } from '@shared/ecs/component';

export class NegativeEffectComponent extends Component {
  constructor() {
    super('negative-effect');
  }

  init(state: Record<string, any>): void {
    //TODO Add init handle
  }
}
